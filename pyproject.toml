[project]
name = "banchi"
version = "0.0.0"
description = "Analisi della struttura argomentativa del dibattito parlamentare italiano (Camera, XIX legislatura)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/banchi"]
